name: haasib_nafsak
description: "تطبيق ديني لمحاسبة النفس اليومية"
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.0
  flutter_secure_storage: ^8.0.0
  encrypt: ^5.0.1
  uuid: ^3.0.7
  intl: ^0.18.0
  fl_chart: ^0.62.0
  path_provider: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/
